% Pandoc LaTeX template for the publishing pipeline.
% Supports both book and article document classes via manuscript.yaml config.
\documentclass[$if(fontsize)$$fontsize$$else$12pt$endif$,$if(papersize)$$papersize$$else$a4paper$endif$,$for(classoption)$$classoption$$sep$,$endfor$]{$documentclass$}

% --- Font and encoding (XeLaTeX) ---
\usepackage{fontspec}
\usepackage{xunicode}
\usepackage{xltxtra}
$if(mainfont)$
\setmainfont{$mainfont$}
$endif$
$if(sansfont)$
\setsansfont{$sansfont$}
$endif$
$if(monofont)$
\setmonofont[Scale=MatchLowercase]{$monofont$}
$endif$

% --- Language ---
$if(lang)$
\usepackage{polyglossia}
\setmainlanguage[$for(polyglossia-lang.options)$$polyglossia-lang.options$$sep$,$endfor$]{$polyglossia-lang.name$}
$endif$

% --- Layout and typography ---
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{setspace}
\onehalfspacing
\usepackage{parskip}

% --- Graphics and figures ---
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
$if(graphics-path)$
\graphicspath{{$graphics-path$}}
$endif$

% --- Tables ---
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}

% --- Lists ---
\usepackage{enumitem}
\setlistdepth{9}

% --- Code listings ---
\usepackage{fancyvrb}
\usepackage{fvextra}
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% --- Links ---
\usepackage[unicode=true]{hyperref}
\hypersetup{
  breaklinks=true,
  pdfborder={0 0 0},
$if(title-meta)$
  pdftitle={$title-meta$},
$endif$
$if(author-meta)$
  pdfauthor={$author-meta$},
$endif$
  colorlinks=true,
  linkcolor=$if(linkcolor)$$linkcolor$$else$blue$endif$,
  citecolor=$if(citecolor)$$citecolor$$else$blue$endif$,
  urlcolor=$if(urlcolor)$$urlcolor$$else$blue$endif$
}
\urlstyle{same}

% --- Bibliography ---
$if(bibliography)$
\usepackage[backend=biber,style=authoryear]{biblatex}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$

% --- Header/footer ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE,RO]{\thepage}
$if(title)$
\fancyhead[RE]{\textit{$title$}}
$endif$
$if(author)$
\fancyhead[LO]{\textit{$for(author)$$author$$sep$, $endfor$}}
$endif$
\renewcommand{\headrulewidth}{0.4pt}

% --- Custom styles ---
$for(header-includes)$
$header-includes$
$endfor$

% --- Document metadata ---
$if(title)$
\title{$title$$if(subtitle)$\\\vspace{0.5em}{\large $subtitle$}$endif$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(date)$
\date{$date$}
$endif$

\begin{document}

$if(title)$
\maketitle
$endif$

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$if(toc)$
{
\setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
\tableofcontents
$if(lof)$
\listoffigures
$endif$
$if(lot)$
\listoftables
$endif$
\clearpage
}
$endif$

$body$

$if(bibliography)$
\printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$
$endif$

$for(include-after)$
$include-after$
$endfor$

\end{document}
